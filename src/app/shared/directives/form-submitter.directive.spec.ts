import { FormSubmitterDirective } from './form-submitter.directive';

describe('FormSubmitterDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSubmitterDirective();
    expect(directive).toBeTruthy();
  });
});
