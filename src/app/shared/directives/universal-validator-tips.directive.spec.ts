import { UniversalValidatorTipsDirective } from './universal-validator-tips.directive';

describe('UniversalValidatorTipsDirective', () => {
  it('should create an instance', () => {
    const directive = new UniversalValidatorTipsDirective();
    expect(directive).toBeTruthy();
  });
});
